---
summary: "CLI reference for `blackcat docs` (search the live docs index)"
read_when:
  - You want to search the live BlackCat docs from the terminal
title: "docs"
---

# `blackcat docs`

Search the live docs index.

```bash
blackcat docs browser extension
blackcat docs sandbox allowHostControl
```
