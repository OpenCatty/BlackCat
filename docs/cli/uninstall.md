---
summary: "CLI reference for `blackcat uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `blackcat uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
blackcat uninstall
blackcat uninstall --all --yes
blackcat uninstall --dry-run
```
